specVersion: "v1"
serviceId: "request-form"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "request-form-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
